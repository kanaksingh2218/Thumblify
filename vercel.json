{
  "buildCommand": "cd client/reactjs && npm install --include=dev && npm run build",
  "outputDirectory": "client/reactjs/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
